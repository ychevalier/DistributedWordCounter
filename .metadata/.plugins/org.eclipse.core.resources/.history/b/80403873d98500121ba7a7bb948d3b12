package Network.FromSlave;

import java.io.IOException;
import java.net.ServerSocket;
import java.net.Socket;

import Controllers.RegisterHandler;
import Listeners.RegisterSlaveListener;
import System.Config;

public class RegisterServer implements Runnable {
	
	private ServerSocket mServerSocket;
	private int mPort;
	private SlaveList mListener;

	public RegisterServer(RegisterSlaveListener listener) {
		mPort = Config.MASTER_REGISTER_PORT;
		mListener = listener;
	}

	@Override
	public void run() {
		try {
			mServerSocket = new ServerSocket(mPort); 
			while (true) {
				Socket client = mServerSocket.accept();
				Thread t = new Thread(new RegisterHandler(client, mListener));
			    t.start();
			}
		} catch (IOException e) {
			e.printStackTrace();
		}
	}

}
